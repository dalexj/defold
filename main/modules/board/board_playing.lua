require "main.modules.board.board_common"

local board_playing = {}

function board_playing.init(self)
end

function board_playing.on_input(self, action_id, action)
end

function board_playing.on_message(self, message_id, message, sender)
end

return board_playing
